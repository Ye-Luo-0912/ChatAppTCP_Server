<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>ChatApp.TcpGateway</AssemblyName>
    <RootNamespace>ChatApp.TcpGateway</RootNamespace>
    <!--
      长连接网关默认走 JIT + TieredPGO：StackExchange.Redis 等依赖仍有裁剪/AOT 警告，
      且峰值吞吐未必优于 JIT。协议 JSON 仍全部走源生成 JsonTypeInfo，便于日后重新启用 AOT。
      若要验证 AOT：临时设 PublishAot=true 并处理 Redis 等依赖警告后再发布。
    -->
    <PublishAot>false</PublishAot>
    <TieredPGO>true</TieredPGO>
    <InvariantGlobalization>true</InvariantGlobalization>
    <ServerGarbageCollection>true</ServerGarbageCollection>
    <ConcurrentGarbageCollection>true</ConcurrentGarbageCollection>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Core\**;Infrastructure\**;Observability\**;tests\**;tools\**;ActorRuntime\**;PushWorker\**;scratch\**" />
    <EmbeddedResource Remove="Core\**;Infrastructure\**;Observability\**;tests\**;tools\**;ActorRuntime\**;PushWorker\**;scratch\**" />
    <None Remove="Core\**;Infrastructure\**;Observability\**;tests\**;tools\**;ActorRuntime\**;PushWorker\**;scratch\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" />
    <PackageReference Include="OpenTelemetry.Exporter.Prometheus.HttpListener" />
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="Core\Core.csproj" />
    <ProjectReference Include="Infrastructure\Infrastructure.csproj" />
    <ProjectReference Include="Observability\ChatApp.TcpGateway.Observability.csproj" />
    <ProjectReference Include="ActorRuntime\ChatApp.ActorRuntime.csproj" />
    <!-- P1-1：Realtime 集成层改为版本化 NuGet 包（本地 feed packages/）。 -->
    <PackageReference Include="ChatApp.Realtime.Integration" />
  </ItemGroup>

  <ItemGroup>
    <None Update="appsettings.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ChatApp.TcpGateway.Tests" />
  </ItemGroup>
</Project>
